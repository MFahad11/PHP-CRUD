# PHP-CRUD
Assignment: CRUD with PHP
Languages used: Html, Css, Bootstrap, PHP 
Overview:
A food panda clone website, but user can’t access it directly he/she must have to sign up/ login to reach 
the website.
Admin panel have separate username and password and only admin would have that to access the 
dashboard.
Used functionality:
Sessions:
There is a single session with two different values like: 
$_Session[“Login”]=2 which is used to access admin panel only, when admin is already logged in, other 
wise doesn’t access.
$_Session[“Login”]=1 which is used to access website, when user is already logged in, other wise doesn’t 
access.
